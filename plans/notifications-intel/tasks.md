# Tasks: notifications-intel

- [x] 1. Core domain (Notification columns, NotificationPreference, Priority/Grouping statics, repo contract)
- [x] 2. Infrastructure repo + DbContext (pipeline, inbox, preferences, quiet release)
- [x] 3. Application CQRS (inbox/unread-count/preference queries+command, DTOs, validator)
- [x] 4. API controller (new endpoints + ownership-auth + RecipientId fixes)
- [x] 5. Migration + regression tests + full build/suite green
